# Secrets — never tracked. The scar tissue is real.
.env
agent_config.yaml

# Runtime state
consent_state.json
band_archive.jsonl
usage_log.jsonl

# Secrets — filled-in ChapterStage roster
chapterstage_agent_config.yaml

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# ChapterStage backend runtime
*.db
chapterstage_backend/static/
chapterstage_backend/.venv/
chapterstage_backend/.local/
static/
