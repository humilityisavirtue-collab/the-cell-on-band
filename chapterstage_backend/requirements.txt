# ChapterStage backend (FastAPI service layer on the Cell-on-Band chassis).
# Install:  python -m venv .venv && .venv/bin/pip install -r requirements.txt
fastapi>=0.110
sqlmodel>=0.0.14
aiosqlite>=0.19
greenlet>=3.0
uvicorn[standard]>=0.27
ngrok>=1.4,<2.0
sse-starlette>=2.0
python-multipart>=0.0.9
httpx>=0.27
pypdf>=4.0
pytest>=8.0
langgraph>=1.0
# For live Band SDK mode, install requirements-live.txt.
